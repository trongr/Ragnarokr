var logger = window.logger

/**
 * Controls game actions, e.g. user input, API calls related to game management.
 */
window.GameController = class {
  constructor() {
    logger.info("one", "two", 3, false)
  }
}
